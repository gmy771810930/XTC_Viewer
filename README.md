# XTC_Viewer
### XTC文件预览器

### 需要Python环境，直接运行即可。
- 打包命令：python -m PyInstaller -F -w xtc_viewer.py

### 默认以文件原分辨率显示，可以选480×800，960×1600，528×792，1056×1584预览效果

## 代码由Deepseek生成
## ai提示词：
```
1.使用 Python 编写一个图形化程序，用于预览 XTC 或 XTCH 格式的电子书文件。
2.程序启动时自动在程序目录下创建 log 文件夹（如果不存在），并在其中生成以 日期时间.log 命名的日志文件，记录所有操作（包括打开文件、翻页、缩放、导出、取消、错误等），便于排障。
3.自动检测并安装缺失的依赖（如 Pillow、numpy），若导入失败则调用 pip 自动安装，安装完成后提示用户重新运行。
4.程序默认以原始分辨率显示图像，并可通过菜单选择以下缩放模式：
原始（实际像素）
X4（480×800）
X4 双倍分辨率（960×1600）
X3（528×792）
X3 双倍分辨率（1056×1584）
5.支持键盘左右方向键（或上下键）翻页，并提供上一页、下一页按钮。
6.在控制栏增加快速跳转页码的输入框和“跳转”按钮，用户可输入页码直接跳转。
7.支持鼠标滚轮操作：当图像完整显示在窗口中时，滚轮触发翻页；当图像超出窗口大小时，滚轮用于滚动 Canvas 视图。
8.支持 Ctrl + 鼠标滚轮缩放图像（放大/缩小），缩放因子范围 0.1～10，缩放后自动切换到“原始”缩放模式，并应用自定义缩放因子。
9.图像显示区域使用 Canvas 加滚动条布局，当图像大于窗口时自动显示水平和垂直滚动条，支持鼠标拖拽滚动条查看图像。
10.每次翻页或缩放后，图像自动在窗口显示区域内居中（居中通过设置滚动条位置实现）。
11.窗口大小改变时，图像自动重新居中。
12.在顶部菜单栏增加“设置”菜单，包含：
“缩放模式”子菜单（将第 4 条的缩放模式移入此处）
“背景颜色”子菜单，提供灰色、白色、黑色和“自定义...”选项。选择自定义时弹出独立窗口，用户可通过 RGB 滑块（红/绿/蓝 0–255）和数值输入框调整背景色，右侧实时预览颜色，点击确定后应用。
13.在菜单栏增加“帮助”菜单，内含“关于”子菜单。点击后弹出对话框，显示软件名称、版本号以及可点击的 GitHub 仓库链接（https://github.com/gmy771810930/XTC_Viewer），点击链接使用系统默认浏览器打开。
14.状态栏分为左右两部分：左侧显示当前页码、显示尺寸、缩放模式等状态信息；右侧显示当前打开的文件名。
“文件”菜单包含以下功能：
打开文件：选择 XTC/XTCH 文件，解析并显示第一页。
保存当前页为图片：弹出保存对话框，用户可选择 PNG 或 JPEG 格式，保存当前显示的页面（原始图像，未缩放）。若未打开文件，则提示“请先打开一个 XTC/XTCH 文件”。
另存为：弹出选项对话框，用于批量导出所有页面为图片序列。对话框包含：
图片格式（PNG / JPEG）
命名方式：两种可选——“原文件名-编号”（例如 example-001.png）或“编号”（例如 001.png）。编号位数根据总页数自动确定（如总页数 15 页则用 2 位，100 页则用 3 位，避免多余前导零）。
保存位置：源目录（在原文件所在目录下创建以原文件名命名的文件夹）或自定义（用户选择目录）。
确认后显示进度对话框，标题为“转换中 - PNG/JPEG 导出”，窗口内显示：当前导出的文件名、保存位置、进度条、百分比，以及“取消”按钮。
导出过程中，每导出完一页均在日志中记录完整的保存路径（如“导出页面 1/100: D:\books\example\example-001.png”）。
导出完成后弹出提示框告知成功导出的页数和保存目录；若用户中途取消，则提示“导出已取消”。
退出：关闭程序。
所有操作（打开文件、翻页、缩放、背景色更改、保存图片、批量导出等）均需写入日志文件，尤其是导出过程中的每个文件路径，以便出现问题时排查。
15.程序界面设计应简洁易用，默认窗口大小 900×700，支持缩放、滚动等交互。
```

实现原理参考了bigbag大佬的epub-to-xtc-converter给大佬点赞：https://github.com/bigbag/epub-to-xtc-converter
